# Temp / test artifacts
_m*
*.log
_scan_results.json

# Built mods — these belong on GitHub Releases / ModHub, not in source control
*.zip

# OS junk
desktop.ini
Thumbs.db
.DS_Store

# Stray empty nested repo from an aborted "own folder" git init (safe to delete)
/FS25_ModSynth/
